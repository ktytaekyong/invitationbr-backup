// C: 메뉴 순서 변경 - 위치 고정된 아이템
const OrderSettingStateFixedItem = ({ children, className }) => {
  return (
    <div className={className}>
      {children}
    </div>
  )
}

export default OrderSettingStateFixedItem;